apiVersion: v1
kind: Service
metadata:
  name: webfront
  # namespace: techtalksns
spec:
  # type: NodePort
  type: LoadBalancer
  ports:
  - port: 80
  # - port: 8088
  selector:
    run: webfront-deployment